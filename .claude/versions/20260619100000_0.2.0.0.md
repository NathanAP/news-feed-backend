# v0.2.0.0 — Project bootstrap

## What was done
Created the Go project skeleton with the core stack and configuration files.

- Initialized Go module (`github.com/nathanap/news-feed-backend`).
- Added core dependencies: Fiber v2, modernc.org/sqlite, godotenv, goose, uuid v7.
- Created `Dockerfile` and `docker-compose.yaml` for containerized deployment.
- Created `.env.example` with all required environment variable keys.
- Created `.gitignore` covering Go binaries, `.env`, and the `db/` folder.
- Created `main.go` with Fiber app startup and a health check endpoint (`GET /v1/health`).
