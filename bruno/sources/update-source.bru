meta {
  name: Update Source
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/v1/sources/:id
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:path {
  id: {{sourceId}}
}

body:json {
  {
    "name": "G1",
    "url": "https://g1.globo.com",
    "url_rss": "https://g1.globo.com/rss/g1/index.xml"
  }
}

docs {
  Todos os campos (name, url, url_rss) são obrigatórios. Retorna 409 em caso de conflito de URL.
}
