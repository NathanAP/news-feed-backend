meta {
  name: Get Article
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/v1/articles/:id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:path {
  id: {{articleId}}
}
